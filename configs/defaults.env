# configs/defaults.env
# Centralized env defaults for entropy_bootstrap_linux

ENTROPY_VERSION="v0.1.3-tested"
DRY_RUN="true"
INSTALL_LOG="/tmp/entropy_install.log"

# Note: this file is intended to be sourced by `setup.sh` when present.
# It intentionally does not export variables; scripts will read the values.
